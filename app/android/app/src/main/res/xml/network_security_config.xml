<?xml version="1.0" encoding="utf-8"?>
<!--
  Copy to: android/app/src/main/res/xml/network_security_config.xml

  WHY YOU NEED THIS
  Android blocks cleartext HTTP by default from API 28 onward. Your dev backend
  runs on http://10.0.2.2:3000 (the host machine as seen from the emulator), so
  without this file every request fails with:

      SocketException: Connection failed ... Cleartext HTTP traffic not permitted

  ...which looks exactly like "the server is down" and is not.

  Note the scope: cleartext is permitted ONLY for the emulator loopback and
  localhost. Production traffic to your real domain stays HTTPS-only, so
  shipping this file by accident does not weaken the release build.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
